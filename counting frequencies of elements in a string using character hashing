#include<bits/stdc++.h>
using namespace std;
int main(){
  string s;
  cin>>s;
  //pre-compute
  int hash[26]={0};
  for(auto i:s){
    hash[i-'a']+=1;
  }
  int q;
  cin>>q;
  while(q--){
    char ch;
    cin>>ch;
    cout << hash[ch-'a'] << endl;
  }
  return 0;
}
